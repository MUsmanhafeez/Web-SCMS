export enum OrganizationType {
  OWNER = `OWNER`,
  NORMAL = `NORMAL`
}

export enum OrganizationStatus {
  ACTIVE = `ACTIVE`,
  SUSPENDED = `SUSPENDED`
}

export enum OrganizationPostType {
  MASJID = `MASJID`,
  OTHER = `OTHER`
}
